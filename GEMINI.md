# Project: BeaBurps

## Gemini Added Memories

- **Change history:** Track changes in a per-repo `HISTORY.md` changelog (create it if it doesn't exist yet). Never copy files into an `Archive/` or `.archive/` folder as a backup method — this project has a real git history; that's the backup.
